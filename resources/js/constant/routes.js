const HOME = {
  INDEX: "/",
};

const NEWS_LIST = {
  INDEX: "/newsList/:id",
};

const NEWS_DETAIL = {
  INDEX: "/newsDetail/:id",
};

const PROGRAM_SCHEDULE = {
  INDEX: "/programSchedules",
};

const PROGRAM_DETAIL = {
  INDEX: "/programDetail/:id",
};

const TEAM_DETAIL = {
  INDEX: "/teamDetail/:id",
}

const CONTACT = {
  INDEX: "/contact"
}

const ERROR = {
  INDEX: "*",
};

export {
  HOME,
  NEWS_LIST,
  NEWS_DETAIL,
  PROGRAM_SCHEDULE,
  PROGRAM_DETAIL,
  TEAM_DETAIL,
  ERROR,
  CONTACT,
};
